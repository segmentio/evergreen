/**
 * Returns a human-readable version of the file size
 * @param {number | null | undefined} sizeInBytes
 * @returns {string | undefined}
 */
const humanizeFileSize = sizeInBytes => sizeInBytes.toString()

export default humanizeFileSize
