# Usage

The Positioner component is a utility component to help with positioning an element to an anchor.
It is used as a helper component throughout Evergreen in components such as [Tooltip](./tooltip) and [Popover](./popover).
